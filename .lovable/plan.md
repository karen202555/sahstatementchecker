

## Change Beta Invite Code

Update the hardcoded beta invite code in `src/pages/Auth.tsx` from `BETA2025` to `BETA2026`.

**File:** `src/pages/Auth.tsx`, line 13  
**Change:** `const BETA_CODE = "BETA2025"` → `const BETA_CODE = "BETA2026"`

Existing users are unaffected — this only applies to new signups.

